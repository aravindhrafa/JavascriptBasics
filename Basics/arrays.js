let selectedColors1 = []; 
// Empty array literal  
// The above code is the empty array initialization


let selectedColors = ['red','blue']; 
// The array can be extended as to our expectation 
// Since JS is dynamic, we can add as many elements inside the array    
// The above could be called the array containing the object 


console.log(selectedColors);

console.log(selectedColors[0])

selectedColors[2]= 'green';
console.log(selectedColors);

selectedColors[3] = 12; 
console.log(selectedColors);

console.log(selectedColors.length);